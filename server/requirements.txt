# [SECURITY] Patched Versions — pinned to exact versions (supply chain protection)
# [SECURITY] I tre CVE (CVE-2024-47874 + CVE-2025-54121 + CVE-2025-62727) sono di
# STARLETTE, fixati in 0.49.1: è lo starlette pin qui sotto a chiuderli, NON la
# versione di fastapi. fastapi 0.121+ accetta già starlette 0.49.x.
#
# [FIX CORS-500] Cap a <0.137: fastapi 0.137.0 ha rifattorizzato include_router()
# (PR fastapi#15745) inserendo oggetti wrapper `_IncludedRouter` SENZA `.path` in
# app.routes. prometheus-fastapi-instrumentator (vedi sotto) fa `route.path` diretto
# per ogni richiesta → AttributeError → 500 su OGNI handler non escluso, preflight
# CORS OPTIONS inclusa (il middleware instrumentator è il più esterno, gira prima
# del CORS) → il browser lo mostra come errore CORS. Il fix upstream del bug
# `_IncludedRouter` esiste solo in instrumentator 8.0.1+, che però richiede
# starlette>=1.0.0 (incompatibile col nostro starlette 0.49.x). Fino a quando non
# si valuta l'upgrade a starlette 1.x, restiamo su fastapi<0.137 (pre-refactor):
# instrumentator 7.x funziona e le metriche restano attive.
# TODO: ri-pinnare a versione esatta dopo primo CI green per supply chain safety.
# TODO: rimuovere il cap <0.137 quando si migra a starlette 1.x + instrumentator 8.x.
fastapi>=0.121.0,<0.137.0
starlette>=0.49.1,<0.50.0
# [SECURITY] GHSA-7669-jjrm-h66r (DoS via malformed headers) fixed in 0.30.0+
uvicorn[standard]==0.34.0
# [SECURITY] CVE-2026-40347 — fixed in 0.0.26
python-multipart==0.0.26
# [SECURITY] pdfplumber 0.11.5 pinnava pdfminer.six==20231228 (vulnerabile a
# CVE-2025-64512 + CVE-2025-70559). Rilasso per trovare pdfplumber più recente
# compatibile con pdfminer.six 20251230 (dove i CVE sono fixati).
# TODO: ri-pinnare a versione esatta dopo primo CI green.
pdfplumber>=0.11.5,<1.0.0
pdfminer.six>=20251230
# [SECURITY] CVE-2026-40192 — fixed in 12.2.0
Pillow==12.2.0
pytesseract==0.3.10
# [SECURITY] Pinned version to prevent supply chain attacks
google-genai==1.0.0
# Bumped from 2.6.0 → 2.10+ for Python 3.13/3.14 wheel availability
# (pydantic-core 2.16.x fails to build from source on Python 3.14 due to
# ForwardRef._evaluate() API change). Range gives pip flexibility to pick
# the latest compatible wheel.
pydantic>=2.10,<3.0
pydantic-settings>=2.6,<3.0
python-dotenv==1.0.1
firebase-admin==6.4.0
# Già dipendenza transitiva di google-auth (firebase-admin); pinnata
# esplicitamente perché ora usata direttamente per decifrare le diete
# cifrate dal client (AES-256-CBC) — vedi app/services/diet_crypto.py.
cryptography>=42.0.0,<47.0.0
email-validator==2.1.0
aiosmtplib==3.0.1
# [SECURITY] CVE-2024-56201 + CVE-2024-56326 (sandbox escape) — fixed in 3.1.5
jinja2==3.1.6
fpdf2==2.7.9
slowapi==0.1.9
# [FIX] slowapi 0.1.9 è incompatibile con limits>=4 (API cambiate): senza
# questo pin un rebuild pulito tira limits 5.x e OGNI endpoint con
# @limiter.limit va in 500 (la preflight CORS inclusa → il browser lo mostra
# come errore CORS). limits è una dep transitiva di slowapi: la vincoliamo qui.
limits>=2.3,<4.0
structlog==24.1.0
sentry-sdk[fastapi]==1.45.1
# Redis cache layer (opzionale - graceful fallback se non configurato)
redis[asyncio]==5.0.1
hiredis==2.3.2
# RQ task queue for async diet parsing (opzionale - graceful fallback se Redis non disponibile)
rq==1.16.1
# APM - Prometheus metrics
# [FIX CORS-500] Il crash "'_IncludedRouter' object has no attribute 'path'" è
# causato da fastapi>=0.137 (non da starlette): vedi il commento sul cap fastapi
# in cima. Con fastapi<0.137 non esistono oggetti `_IncludedRouter` in app.routes,
# quindi 7.x (routing.get_route_name fa `route.path` diretto) NON crasha.
# Il cap <8.0.0 resta OBBLIGATORIO: 8.0.x richiede starlette>=1.0.0 (incompatibile
# col nostro starlette 0.49.x) → senza il cap pip non risolve.
# TODO: passare a >=8.0.1 solo insieme all'upgrade starlette 1.x + rimozione cap fastapi.
prometheus-fastapi-instrumentator>=7.0.0,<8.0.0
prometheus-client==0.20.0
